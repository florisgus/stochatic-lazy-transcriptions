4. Wiener Process Exercises.md

# Wiener Process: Exercises and Properties

---

## **Stationary and Independent Increments**
- **Wiener Process Definition**:
  - $W(t)$ is a Wiener process if:
    1. $W(0) = 0$.
    2. Increment $W(t_i) - W(t_{i-1}) \sim N(0, t_i - t_{i-1})$.
    3. Increments are independent.

- **Joint Distribution**:
  $$
  P(W(t_1) = w_1, \ldots, W(t_k) = w_k) = P(W(t_1) = w_1) \prod_{i=2}^k P(W(t_i) - W(t_{i-1}) = w_i - w_{i-1})
  $$

---

## **Scaling a Brownian Process**
- If $W(t)$ is a Wiener process, then:
  $$
  X(t) = \frac{1}{\sqrt{c}} W(ct)
  $$
  is also a Wiener process for $c > 0$.

- **Verification**:
  - $X(0) = 0$.
  - Increment $X(t_2) - X(t_1) \sim N(0, t_2 - t_1)$.
  - Independence of increments is preserved.

---

## **Gaussian Processes**
- A stochastic process $\{X(t), t \in T\}$ is **Gaussian** if the random vector $(X(t_1), \ldots, X(t_n))$ has a multivariate normal distribution for any $n$ and $t_1, \ldots, t_n$.

- **Example**:
  $$
  X(t) = tW(t)
  $$
  is a Gaussian process if $W(t)$ is a Wiener process.

---

## **Ito's Lemma and Formula**
- **Ito's Lemma**:
  For $X(t)$ satisfying $dX(t) = f(X(t))dt + g(X(t))dW(t)$, and a smooth function $u(t, x)$:
  $$
  du(t, X(t)) = \left( u_t + u_x f + \frac{1}{2} u_{xx} g^2 \right) dt + u_x g \, dW(t)
  $$

- **Example 1: Ornstein-Uhlenbeck Process**:
  $$
  dX = \lambda (\mu - X(t)) dt + \sigma dW(t)
  $$
  This is a mean-reverting process.

- **Example 2: Geometric Brownian Motion**:
  $$
  dX = \mu X \, dt + \sigma X \, dW(t)
  $$
  Solution:
  $$
  X(t) = X(0) e^{(\mu - \frac{\sigma^2}{2})t + \sigma W(t)}
  $$

---

## **Phase Transitions and Ginzburg-Landau**
- **SDE for Phase Transitions**:
  $$
  dx = (\gamma x + \xi^2 x - \lambda x^3) dt + \sigma x \, dW(t)
  $$
  - For small $x$, $f(x) \approx \gamma x$.

- **Double Well Potential**:
  - Deterministic ODE:
    $$
    \frac{dx}{dt} = -V'(x(t))
    $$
  - With noise, transitions between wells are possible.

---

## **Euler-Maruyama Scheme**
- **Discretization**:
  $$
  X_{n+1} = X_n + \Delta t \, f(X_n) + \Delta W_n \, g(X_n)
  $$
  where $\Delta W_n = W(t_{n+1}) - W(t_n)$.

- **Pseudocode**:
  ```python
  Δt = T / N
  X[0] = x0
  for n in 0 to N-1:
      ξ_n = sample from N(0,1)
      X[n+1] = X[n] + Δt * f(X[n]) + √(Δt) * ξ_n * g(X[n])
```


# Next Topic

[[5. Euler-Maruyama and Convergence]]

Tags: #WienerProcess #ItoLemma #EulerMaruyama #GaussianProcess #SDEs
