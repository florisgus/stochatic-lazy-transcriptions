# Euler-Maruyama Scheme and Convergence

---

## **Euler-Maruyama Scheme**
- **Discretization**:
  $$
  X_{n+1} = X_n + \Delta t \, f(X_n) + \Delta W_n \, g(X_n)
  $$
  where ( $\Delta W_n = W(t_{n+1}) - W(t_n)$.

- **Pseudocode**:
  ```python
  Δt = T / N
  X[0] = x0
  for n in 0 to N-1:
      ξ_n = sample from N(0,1)
      X[n+1] = X[n] + Δt * f(X[n]) + √(Δt) * ξ_n * g(X[n])
```
Convergence: Weak and Strong
Weak Convergence


Definition:
$$
e_{\Delta t}^{\text{weak}} = \sup_{0 \leq t \leq T} |E[\phi(X_n)] - E[\phi(X(t))]|
$$
Measures how well the method approximates the expected value $E[\phi(X(t))]$.


Order of Convergence:

A method has weak order ( $p$ ) if $e_{\Delta t}^{\text{weak}} \leq K (\Delta t)^p$.


Example:
For  $dX = \mu X , dt + \sigma X , dW(t)$ , the weak error can be estimated by comparing $E[X_n] ) and ( E[X(T)]$.


Strong Convergence


Definition:
$$
e_{\Delta t}^{\text{strong}} = \sup_{0 \leq t \leq T} E[|X_n - X(t)|]
$$
Measures the pathwise accuracy of the numerical method.


Order of Convergence:

A method has strong order ( p ) if $e_{\Delta t}^{\text{strong}} \leq K (\Delta t)^p$.


Markov Inequality:
$$
P(|X_n - X(t)| > \epsilon) \leq \frac{E[|X_n - X(t)|]}{\epsilon}
$$


Feynman-Kac Formula


PDE Connection:
The solution to the SDE $dX(t) = f(X(t)) dt + g(X(t)) dW(t)$is related to the solution of the PDE:
$$
u_t + f(x) u_x + \frac{1}{2} g^2(x) u_{xx} = 0
$$
with final condition $u(T, x) = \phi(x)$.


Feynman-Kac:
$$
u(t, x) = E[\phi(X(T)) | X(t) = x]
$$


Next Topic

[[6. Fokker-Planck and Ito-Stratonovich]]

Tags: #EulerMaruyama #WeakConvergence #StrongConvergence #FeynmanKac #SDEs
