 Fokker-Planck Equation and Ito-Stratonovich Integral

---

## **Fokker-Planck Equation**
- **Purpose**:
  Describes the evolution of the probability density function $p(x, t)$ of the solution to an SDE.

- **General Form**:
  $$
  \frac{\partial p}{\partial t} = -\frac{\partial}{\partial x} (f(x) p) + \frac{1}{2} \frac{\partial^2}{\partial x^2} (g^2(x) p)
  $$

- **Derivation**:
  - Use the Chapman-Kolmogorov equation and Taylor expansion.
  - For $dX_t = f(X_t) dt + g(X_t) dW_t$, the Fokker-Planck equation is:
    $$
    \frac{\partial p}{\partial t} = -\frac{\partial}{\partial x} (f p) + \frac{1}{2} \frac{\partial^2}{\partial x^2} (g^2 p)
    $$

- **Example 1: Ornstein-Uhlenbeck Process**:
  $$
  dX_t = -\lambda X_t \, dt + \sigma \, dW_t
  $$
  Steady-state solution:
  $$
  p(x) = \sqrt{\frac{\lambda}{\pi \sigma^2}} e^{-\frac{\lambda x^2}{\sigma^2}}
  $$

- **Example 2: Population Model**:
  $$
  dX_t = \alpha X_t (K - X_t) dt + \sigma X_t dW_t
  $$
  Steady-state solution involves solving the Fokker-Planck equation for the given $f$ and $g$.

---

## **Ito vs. Stratonovich Integral**
- **Ito Integral**:
  $$
  \int_0^T h(t) \, dW(t)
  $$
  - Uses the left endpoint for evaluation.
  - Does not follow classical chain rule.

- **Stratonovich Integral**:
  $$
  \int_0^T h(t) \circ dW(t)
  $$
  - Uses the midpoint for evaluation.
  - Follows the classical chain rule.

- **Conversion**:
  $$
  \int_0^T h(X(t)) \circ dW(t) = \int_0^T h(X(t)) \, dW(t) + \frac{1}{2} \int_0^T h'(X(t)) g(X(t)) \, dt
  $$

---

## **Higher-Order Integration Methods**
- **Milstein Scheme**:
  $$
  X_{n+1} = X_n + \Delta t \, f(X_n) + \Delta W_n \, g(X_n) + \frac{1}{2} g(X_n) g'(X_n) (\Delta W_n^2 - \Delta t)
  $$
  - Strong order of convergence: $O(\Delta t)$.

- **Heun Scheme (for Stratonovich SDEs)**:
  $$
  X_{n+1} = X_n + \frac{\Delta t}{2} (f(X_n) + f(X_{n+1}^*)) + \frac{\Delta W_n}{2} (g(X_n) + g(X_{n+1}^*))
  $$
  where $X_{n+1}^* = X_n + \Delta t \, f(X_n) + \Delta W_n \, g(X_n)$.

---

## **Next Topic**
- [[4. Wiener Process Exercises]] (for recap)
- [[5. Euler-Maruyama and Convergence]] (for numerical methods)

---
**Tags**: `#FokkerPlanck #ItoIntegral #StratonovichIntegral #MilsteinScheme #SDEs`